import base from '@jlg/oxfmt' with { type: 'json' };

// The @jgeschwendt/oxfmt base (printWidth, sortPackageJson) is spread in LIVE —
// imported straight from the package, not copied. The former .oxfmtrc.json kept a
// hand-synced "mirrored pair" of those values; this import removes that duty (the
// base is the single source of truth). oxfmt has no `extends` and does NOT
// auto-discover a JS/TS config (verified 2026-07-20, oxfmt 0.59), so this file is
// wired with `-c oxfmt.config.ts` at every invocation site (lefthook, CI, the
// fmt/fmt:check scripts) and evaluated under Node.
export default {
  ...base,
  // public/background: wasm-bindgen output, regenerated by `bun run
  // background:build` — never hand-format it. .vscode: editor settings.
  ignorePatterns: ['.vscode/**', 'public/background/**'],
  singleQuote: true,
  sortTailwindcss: {
    stylesheet: './src/app/global.css',
  },
};
