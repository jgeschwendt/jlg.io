# jlg.io

```stele
kind: system
purpose: Personal web profile — Next 16 App Router on Bun, deployed to Vercel, with a Rust/Bevy wasm animated background and a strict per-request CSP. No backend; the site IS the product.
commands:
  build: bun run build
  dev: bun run dev
  lint: bun run lint
  fmt:check: bun run fmt:check
  tsc: bunx tsc --noEmit
  background:build: bun run background:build
invariants:
  - claim: ALIAS CONSUMPTION — @jlg/* names in package.json/configs are npm aliases to npm:@jgeschwendt/* on GitHub Packages (interim scope; the npmjs @jlg org is the end state); .npmrc + NODE_AUTH_TOKEN are required even for public reads (anonymous 401)
    anchor: lm:alias-consumption
  - claim: FORMAT POLICY — printWidth is deliberately absent (oxfmt default 100; the repo predates the base's former 80); singleQuote comes from the @jlg/oxfmt base via defineConfig
    anchor: lm:format-policy
hazards:
  - claim: TS7 BUILD — Next 16.2 cannot drive TypeScript 7's native API in-build; @typescript/native-preview is the escape hatch that makes `next build` pass; ignoreBuildErrors alone does NOT help (dependency detection still crashes); the CI `bunx tsc --noEmit` step is the real type gate
    anchor: lm:ts7-build
  - claim: LEFTHOOK FOOTGUN — oxlint (exit 1, 'No files found to lint') and oxfmt 0.62+ (exit 2, 'Expected at least one target file') both fail the commit when every staged file is ignored; --no-error-on-unmatched-pattern is load-bearing on both hooks in lefthook.toml
    anchor: lm:lefthook-unmatched
  - claim: CONFIG RUNTIME — oxlint.config.ts / oxfmt.config.ts are auto-discovered; oxlint's evaluates under Node >=22.18 even via bunx, so mise pins node for CI
    anchor: lm:config-runtime
  - claim: WASM OUTPUT — public/background is wasm-bindgen output regenerated by `bun run background:build` — never hand-edited, ignored by both lint and fmt
    anchor: lm:wasm-output
```

<!-- stele:begin router -->

## Hazards (5 active)

- ⚠ `/`: CONFIG RUNTIME — oxlint.config.ts / oxfmt.config.ts are auto-discovered; oxlint's evaluates under Node >=22.18 even via bunx, so mise pins node for CI (→ lm:config-runtime)
- ⚠ `/`: LEFTHOOK FOOTGUN — oxlint (exit 1, 'No files found to lint') and oxfmt 0.62+ (exit 2, 'Expected at least one target file') both fail the commit when every staged file is ignored; --no-error-on-unmatched-pattern is load-bearing on both hooks in lefthook.toml (→ lm:lefthook-unmatched)
- ⚠ `src`: PROXY SHORT-CIRCUIT — a middleware handler aborts the chain by THROWING a NextResponse (caught in createMiddleware and returned as-is); a normally-returned response is merged into the shared response, not short-circuited (→ lm:proxy-short-circuit)
- ⚠ `/`: TS7 BUILD — Next 16.2 cannot drive TypeScript 7's native API in-build; @typescript/native-preview is the escape hatch that makes `next build` pass; ignoreBuildErrors alone does NOT help (dependency detection still crashes); the CI `bunx tsc --noEmit` step is the real type gate (→ lm:ts7-build)
- ⚠ `/`: WASM OUTPUT — public/background is wasm-bindgen output regenerated by `bun run background:build` — never hand-edited, ignored by both lint and fmt (→ lm:wasm-output)

## Map

| node | kind      | purpose                                                                                                                                             | unfold                                       |
| ---- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| src  | container | Next App Router UI (app/ pages, components/) plus the Edge middleware chain under server/proxy. Rendering is fully dynamic — see the CSP invariant. | `stele unfold src` · or read `src/AGENTS.md` |

## Indexes

All invariants: `.stele/index/invariants.md` · all hazards: `.stele/index/hazards.md`

## Engine

`stele` CLI available → `stele root | unfold <id> | invariants --touching <path> | hazards | nodes --kind <k>`. MCP: `stele serve`.
No engine → everything above is complete; nested AGENTS.md files carry the detail (nearest file wins).
<!-- stele:end -->

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
