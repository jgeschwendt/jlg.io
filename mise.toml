[tools]
bun = "1.3.14"
# node: oxlint evaluates the TypeScript `oxlint.config.ts` under Node (>=22.18),
# not bun — mise-action installs this in CI so `bun run lint` can load the config.
node = "24"
rust = "latest"

[env]
_.file = ".env"
