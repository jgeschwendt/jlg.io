# environments and secrets
.env
.env.*
.secrets
src/app/secrets/

src/**/*.js
src/**/*.jsx

# dependencies
node_modules

# caches
.next
.vercel
crates/background/target

# coverage (raw Istanbul dumps, merged reports, Playwright run output) —
# root-anchored: a bare `coverage` also matches src/app/api/coverage/, which is
# how the route handler silently never got committed the first time.
/.nyc_output/
/coverage/
/playwright-report/
/test-results/

# outputs
*.tsbuildinfo
next-env.d.ts

# tools
.DS_Store
.vscode
# visor (dev proxy) data dir
.visor/
